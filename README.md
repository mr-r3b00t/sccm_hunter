# sccm_hunter

Key features:

Runs entirely from domain user context
Safe error handling (won't crash on permission denied)
Multiple discovery methods (WMI, registry, AD LDAP, HTTP, DNS)
Outputs both human-readable and JSON formats
Color-coded console output for easy reading


use at own risk
